export enum DefaultLocationForTestFiles {
    SAME_AS_SOURCE_FILE = "same location as source file",
    PROJECT_ROOT = "project root",
    SAME_AS_SOURCE_FILE_WITH_TEST_DIRECTORY = "same location as source file with test directory",
}

export const DEFAULT_TEST_DIRECTORY_NAME: string = "tests";
export const DEFAULT_TEST_FILES_SUFFIX: string = "test";
