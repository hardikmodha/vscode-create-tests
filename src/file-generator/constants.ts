export enum DefaultLocationForNewFiles {
  SAME_AS_SOURCE_FILE = "same location as source file",
  PROJECT_ROOT = "project root",
  SAME_AS_SOURCE_FILE_WITH_NEW_DIRECTORY = "same location as source file with new directory",
}

export const DEFAULT_FILE_SUFFIX: string = "test";
export const DEFAULT_DIR_NAME: string = "__tests__";
